<%= form_with(model: geolocation, local: true) do |form| %>
  <% if geolocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geolocation.errors.count, "error") %> prohibited this geolocation from being saved:</h2>

      <ul>
      <% geolocation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :geolocation_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :geolocation_longitude %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :geolocation_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
